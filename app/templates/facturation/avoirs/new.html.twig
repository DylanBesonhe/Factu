{% extends 'layout.html.twig' %}

{% block title %}Nouvel avoir libre - Factu{% endblock %}

{% block page_title %}Nouvel avoir libre{% endblock %}

{% block page_actions %}
    <a href="{{ path('app_facturation_avoirs') }}" class="px-4 py-2 text-sm text-gray-600 hover:bg-gray-100 rounded-lg">
        Retour a la liste
    </a>
{% endblock %}

{% block content %}
<div class="max-w-2xl mx-auto">
    <div class="bg-white rounded-lg shadow">
        <div class="p-6 border-b border-gray-200">
            <h2 class="text-lg font-medium text-gray-900">Informations de l'avoir</h2>
            <p class="mt-1 text-sm text-gray-500">Creez un avoir libre (non lie a une facture existante).</p>
        </div>

        {{ form_start(form, {'attr': {'class': 'p-6 space-y-6'}}) }}
            <div>
                {{ form_label(form.client, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.client) }}
                {{ form_errors(form.client) }}
                <p class="mt-1 text-sm text-gray-500">Le client beneficiaire de l'avoir</p>
            </div>

            <div>
                {{ form_label(form.motif, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.motif) }}
                {{ form_errors(form.motif) }}
                <p class="mt-1 text-sm text-gray-500">Expliquez la raison de cet avoir</p>
            </div>

            <div class="bg-orange-50 border border-orange-200 rounded-lg p-4">
                <div class="flex">
                    <svg class="w-5 h-5 text-orange-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div class="ml-3">
                        <h3 class="text-sm font-medium text-orange-800">Avoir libre</h3>
                        <div class="mt-2 text-sm text-orange-700">
                            <p>Un avoir libre n'est pas lie a une facture existante. Il sera cree en brouillon et vous pourrez ensuite :</p>
                            <ul class="mt-2 list-disc list-inside space-y-1">
                                <li>Ajouter des lignes de credit</li>
                                <li>Valider l'avoir pour generer le numero (AV-XXXX)</li>
                                <li>L'envoyer et le marquer comme paye</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <div class="flex justify-end gap-4 pt-4 border-t border-gray-200">
                <a href="{{ path('app_facturation_avoirs') }}" class="px-4 py-2 text-sm text-gray-600 hover:bg-gray-100 rounded-lg">
                    Annuler
                </a>
                <button type="submit" class="px-4 py-2 text-sm text-white bg-orange-600 hover:bg-orange-700 rounded-lg">
                    Creer l'avoir
                </button>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
