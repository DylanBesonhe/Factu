{% extends 'layout.html.twig' %}

{% block title %}Nouvelle facture - Factu{% endblock %}

{% block page_title %}Nouvelle facture manuelle{% endblock %}

{% block page_actions %}
    <a href="{{ path('app_facturation_manuelles') }}" class="px-4 py-2 text-sm text-gray-600 hover:bg-gray-100 rounded-lg">
        Retour a la liste
    </a>
{% endblock %}

{% block content %}
<div class="max-w-2xl mx-auto">
    <div class="bg-white rounded-lg shadow">
        <div class="p-6 border-b border-gray-200">
            <h2 class="text-lg font-medium text-gray-900">Informations de la facture</h2>
            <p class="mt-1 text-sm text-gray-500">Selectionnez un client pour creer une facture ponctuelle.</p>
        </div>

        {{ form_start(form, {'attr': {'class': 'p-6 space-y-6'}}) }}
            <div>
                {{ form_label(form.client, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.client) }}
                {{ form_errors(form.client) }}
                <p class="mt-1 text-sm text-gray-500">Le client a facturer</p>
            </div>

            <div>
                {{ form_label(form.contrat, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.contrat) }}
                {{ form_errors(form.contrat) }}
                <p class="mt-1 text-sm text-gray-500">Si lie a un contrat, l'emetteur du contrat sera utilise</p>
            </div>

            <div>
                {{ form_label(form.commentaire, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
                {{ form_widget(form.commentaire) }}
                {{ form_errors(form.commentaire) }}
                <p class="mt-1 text-sm text-gray-500">Commentaire interne (non visible sur la facture)</p>
            </div>

            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                <div class="flex">
                    <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div class="ml-3">
                        <h3 class="text-sm font-medium text-blue-800">Information</h3>
                        <div class="mt-2 text-sm text-blue-700">
                            <p>La facture sera creee en brouillon. Vous pourrez ensuite :</p>
                            <ul class="mt-2 list-disc list-inside space-y-1">
                                <li>Ajouter des lignes de facturation</li>
                                <li>Modifier les dates et montants</li>
                                <li>Valider la facture pour generer le numero definitif</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <div class="flex justify-end gap-4 pt-4 border-t border-gray-200">
                <a href="{{ path('app_facturation_manuelles') }}" class="px-4 py-2 text-sm text-gray-600 hover:bg-gray-100 rounded-lg">
                    Annuler
                </a>
                <button type="submit" class="px-4 py-2 text-sm text-white bg-blue-600 hover:bg-blue-700 rounded-lg">
                    Creer la facture
                </button>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
